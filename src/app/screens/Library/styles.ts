import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  booksList: {
    margin: 16,
  },
  emptyStateContainer: {
    alignItems: 'center',
    flex:1,
    justifyContent: 'center',
    margin: 16,
  },
  emptyStateInfo: {
    fontSize: 24
  }
});
